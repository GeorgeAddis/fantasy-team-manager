# Merge these into backend/.env after Laravel bootstrap (or use as reference).
# Docker backend service also sets DB_* via environment — .env still needs APP_KEY.

DB_CONNECTION=pgsql
DB_HOST=postgres
DB_PORT=5432
DB_DATABASE=fantasy_team_manager
DB_USERNAME=fantasy
DB_PASSWORD=fantasy_secret
