# Fantasy Team Manager — Postgres + nginx + PHP-FPM (frontend runs on host via npm)
services:
  postgres:
    image: postgres:16-alpine
    container_name: ftm-postgres
    restart: unless-stopped
    environment:
      POSTGRES_DB: fantasy_team_manager
      POSTGRES_USER: fantasy
      POSTGRES_PASSWORD: fantasy_secret
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U fantasy -d fantasy_team_manager"]
      interval: 5s
      timeout: 5s
      retries: 5

  backend:
    build:
      context: .
      dockerfile: docker/php/Dockerfile
    container_name: ftm-backend
    working_dir: /var/www
    volumes:
      - ./backend:/var/www
    expose:
      - "9000"
    environment:
      APP_ENV: local
      APP_DEBUG: "true"
      DB_CONNECTION: pgsql
      DB_HOST: postgres
      DB_PORT: "5432"
      DB_DATABASE: fantasy_team_manager
      DB_USERNAME: fantasy
      DB_PASSWORD: fantasy_secret
    depends_on:
      postgres:
        condition: service_healthy
    command: >
      sh -c "
      if [ ! -f artisan ]; then
        echo 'Missing backend/Laravel. Run: docker compose --profile init run --rm composer-init';
        exit 1;
      fi;
      php artisan migrate --force 2>/dev/null || true;
      php-fpm
      "

  nginx:
    image: nginx:1-alpine
    container_name: ftm-nginx
    restart: unless-stopped
    ports:
      - "8000:80"
    volumes:
      - ./backend:/var/www
      - ./docker/nginx/default.conf:/etc/nginx/conf.d/default.conf:ro
    depends_on:
      - backend

  # One-shot: creates ./backend Laravel app if missing (run once)
  composer-init:
    image: composer:latest
    profiles: ["init"]
    working_dir: /app
    volumes:
      - .:/app
    entrypoint: ["/bin/sh", "/app/docker/composer-init.sh"]

volumes:
  postgres_data:
